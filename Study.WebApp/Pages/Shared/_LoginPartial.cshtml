<ul class="navbar-nav">
    @{
        if (SignInManager.IsSignedIn(User))
        {
            var user = await UserManager.GetUserAsync(User);
            <li class="nav-item">
                <a class="nav-link text-dark" asp-page="/Account/UpdateInfo" title="个人中心">@user.UserName</a>
            </li>

            <li class="nav-item">
                <a class="nav-link text-dark" asp-page="/Account/Login" asp-page-handler="Logout" title="注销">注销</a>
            </li>


            @*<li class="nav-item">
                    <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post" >
                        <button  type="submit" class="nav-link btn btn-link text-dark">注销</button>
                    </form>
                </li>*@
        }
        else
        {
            <li class="nav-item">
                <a class="nav-link text-dark" asp-page="/Account/Register">注册</a>
            </li>

            <li class="nav-item">
                <a class="nav-link text-dark" asp-page="/Account/Login">登录</a>
            </li>
        }
    }
</ul>
